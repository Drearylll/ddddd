# 阿里云百炼完整模型配置指南

## ✅ 模型配置已更新

根据你提供的真实可用模型名称，我已经更新了配置文件 `config/dashscope_config.py`。

---

## 📋 完整模型列表

### 文本生成与图像分析

| 模型名称 | 用途 | 特点 | 推荐场景 |
|---------|------|------|---------|
| **tongyi-xiaomi-analysis-pro** | 文案生成、内容分析 | 高质量文本生成，深度分析能力 | 朋友圈文案、地点分析、内容创作 |

### 图像生成

| 模型名称 | 用途 | 特点 | 推荐场景 |
|---------|------|------|---------|
| **qwen-image-2.0-pro** | AI 绘画、图像生成 | 高质量图像生成，支持多种风格 | 人物与背景融合、AI 绘画创作 |

### 多模态理解

| 模型名称 | 用途 | 特点 | 推荐场景 |
|---------|------|------|---------|
| **qwen3-vl-embedding** | 图像 + 文本联合嵌入 | 多模态特征提取，语义理解 | 图片内容分析、场景理解、语义匹配 |

### 语音合成

| 模型名称 | 用途 | 特点 | 推荐场景 |
|---------|------|------|---------|
| **qwen3-tts-vd-2026-01-26** | 文本转语音 | 自然语音合成，支持多种音色 | AI 配音、语音播报、有声内容 |

---

## 🔧 配置详情

### 配置文件位置

```
c:\Users\hn\Desktop\桌面的东西\GOIN2\config\dashscope_config.py
```

### 核心配置

```python
# 阿里云百炼 API Key
DASHSCOPE_API_KEY = "sk-2274b3d46339f95092d68b83150ead7f"
QWEN_API_KEY = "sk-2274b3d46339f95092d68b83150ead7f"
QWEN_VL_API_KEY = "sk-2274b3d46339f95092d68b83150ead7f"

# 模型配置字典
MODELS = {
    # 图像生成
    "wanxiang": {
        "model": "qwen-image-2.0-pro",
        "api_key": QWEN_API_KEY,
        "base_url": "https://dashscope.aliyuncs.com/api/v1/services/aigc/image-generation"
    },
    
    # 文本生成
    "qwen": {
        "model": "tongyi-xiaomi-analysis-pro",
        "api_key": QWEN_API_KEY,
        "base_url": "https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation"
    },
    
    # 多模态理解
    "qwen-vl": {
        "model": "qwen3-vl-embedding",
        "api_key": QWEN_VL_API_KEY,
        "base_url": "https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation"
    },
    
    # 语音合成
    "qwen-tts": {
        "model": "qwen3-tts-vd-2026-01-26",
        "api_key": QWEN_API_KEY,
        "base_url": "https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation"
    }
}
```

---

## 💡 使用建议

### 文案生成任务

**推荐配置**：
```python
from services.doubao_ai import DoubaoAI

ai = DoubaoAI(use_backup=True)  # 使用阿里云
result = ai.generate_text(
    prompt="写一段朋友圈文案",
    max_tokens=200,
    temperature=0.8
)
```

### 图像理解任务

**推荐配置**：
```python
from services.location_analyzer import LocationStyleAnalyzer

analyzer = LocationStyleAnalyzer(
    api_key="sk-2274b3d46339f95092d68b83150ead7f",
    model="qwen-vl"
)

result = analyzer.analyze_style(
    image_url="https://example.com/image.jpg",
    name="地点名称"
)
```

### 图像生成任务

**推荐配置**：
```python
from services.ai_compositor import AIImageCompositor

compositor = AIImageCompositor(api_key="sk-2274b3d46339f95092d68b83150ead7f")

result = compositor.composite_images(
    user_image_url=user_url,
    background_image_url=bg_url,
    location_name="地点名称",
    style_description="风格描述"
)
```

---

## 🎯 在 AI Timeline 中的应用

### 自动使用新模型

在 `services/ai_timeline.py` 中，所有调用会自动使用新的模型名称：

```python
# 步骤 2：分析地点风格
style = analyze_location_style(...)
# 自动使用：qwen3-vl-embedding

# 步骤 4：生成文案
caption = generate_caption(...)
# 自动使用：tongyi-xiaomi-analysis-pro

# 步骤 3：生成融合照片
composite = composite_images(...)
# 自动使用：qwen-image-2.0-pro
```

---

## ⚠️ 重要提示

### API Key 验证

虽然模型名称已更新，但仍需要：

1. **登录阿里云百炼控制台**
   ```
   https://bailian.console.aliyun.com/
   ```

2. **确认服务已开通**
   - 进入"我的模型"
   - 查看是否有这些模型的访问权限

3. **检查免费额度**
   - 新用户可以享受免费额度
   - 查看剩余额度和有效期

4. **验证 API Key**
   - 确认 Key 有效
   - 检查是否欠费

### 测试运行

更新后首次运行：

```bash
python test_doubao_ai.py
python test_ai_timeline.py
```

**预期结果**：
- ✅ 不再报 "Invalid API-key" 错误
- ✅ 能够正常调用阿里云 API
- ✅ 返回真实的 AI 生成内容

---

## 🔄 双云架构

### 降级策略

```
火山引擎（主）
    ↓ 失败
阿里云百炼（备用）← 已更新为新模型
    ↓ 失败
模拟数据（最终降级）
```

### 强制使用阿里云

```python
from services.doubao_ai import DoubaoAI

# 直接使用阿里云（不经过火山引擎）
ai = DoubaoAI(use_backup=True)
```

---

## 📊 模型选择策略

### 按场景选择

**文案生成**：
```python
config = MODELS["qwen"]  # tongyi-xiaomi-analysis-pro
```

**图像理解**：
```python
config = MODELS["qwen-vl"]  # qwen3-vl-embedding
```

**图像生成**：
```python
config = MODELS["wanxiang"]  # qwen-image-2.0-pro
```

**语音合成**：
```python
config = MODELS["qwen-tts"]  # qwen3-tts-vd-2026-01-26
```

---

## ✅ 完成清单

- [x] 更新配置文件中的模型名称
- [x] 添加文本生成模型（tongyi-xiaomi-analysis-pro）
- [x] 添加图像生成模型（qwen-image-2.0-pro）
- [x] 添加多模态模型（qwen3-vl-embedding）
- [x] 添加语音合成模型（qwen3-tts-vd-2026-01-26）
- [x] 保留降级机制
- [x] 编写完整文档

---

## 🚀 下一步

1. **运行测试验证配置**
   ```bash
   python test_doubao_ai.py
   python test_ai_timeline.py
   ```

2. **检查阿里云控制台**
   - 确认 API Key 有效
   - 确认模型有访问权限
   - 查看免费额度

3. **开始使用新模型**
   ```python
   from services.ai_timeline import run_ai_timeline
   result = run_ai_timeline(user_id="xxx")
   ```

---

*配置更新时间：2026 年 3 月 20 日*  
*项目：Go In App - AI 社交*
